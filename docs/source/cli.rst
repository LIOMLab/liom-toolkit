=============
CLI Reference
=============

The LIOM Toolkit ships seven ``liom-*`` command-line tools, registered as
console scripts in ``pyproject.toml`` under ``[project.scripts]``. Each tool
is a thin wrapper over a library function: the script module defines
``_build_argument_parser()`` (which builds the argparse tree without
importing heavy dependencies) and ``main()`` (which lazy-imports the heavy
deps and wires an optional ``--dask_scheduler`` argument to
``dask_client_manager.set_client(...)`` before calling domain logic).

The reference below is auto-generated by `sphinx-argparse
<https://sphinx-argparse.readthedocs.io/>`_ from each script's live
``_build_argument_parser()`` function, so the documented options always
match the code — there is no hand-maintained argument table to drift.

Run any tool with ``--help`` for the authoritative options list:

.. code-block:: bash

   liom-convert-hdf5-to-zarr --help

Conversion
----------

liom-convert-hdf5-to-zarr
~~~~~~~~~~~~~~~~~~~~~~~~~

.. argparse::
   :module: liom_toolkit.scripts.liom_convert_hdf5_to_zarr
   :func: _build_argument_parser
   :prog: liom-convert-hdf5-to-zarr

Mask generation
---------------

liom-create-mask
~~~~~~~~~~~~~~~~

.. argparse::
   :module: liom_toolkit.scripts.liom_create_mask
   :func: _build_argument_parser
   :prog: liom-create-mask

Segmentation
------------

liom-segment-2d
~~~~~~~~~~~~~~~

.. argparse::
   :module: liom_toolkit.scripts.liom_segment_2d
   :func: _build_argument_parser
   :prog: liom-segment-2d

Registration
------------

liom-align-annotations
~~~~~~~~~~~~~~~~~~~~~~

.. argparse::
   :module: liom_toolkit.scripts.liom_align_annotations
   :func: _build_argument_parser
   :prog: liom-align-annotations

liom-build-template
~~~~~~~~~~~~~~~~~~~

.. argparse::
   :module: liom_toolkit.scripts.liom_build_template
   :func: _build_argument_parser
   :prog: liom-build-template

Statistics
----------

liom-compute-slice-metrics
~~~~~~~~~~~~~~~~~~~~~~~~~~

.. argparse::
   :module: liom_toolkit.scripts.liom_compute_slice_metrics
   :func: _build_argument_parser
   :prog: liom-compute-slice-metrics

Deep learning
-------------

liom-train-model
~~~~~~~~~~~~~~~~

.. argparse::
   :module: liom_toolkit.scripts.liom_train_model
   :func: _build_argument_parser
   :prog: liom-train-model

.. note::

   The ``liom-train-model`` and ``liom-segment-2d`` (U-Net mode) tools
   require the ``ai`` extra (``pip install liom-toolkit[ai]``). The
   ``liom-build-template`` and ``liom-align-annotations`` tools require the
   ``antspy`` extra (``pip install liom-toolkit[antspy]``). See
   :doc:`getting_started` for the full extras matrix.
